let name = " raymond dwipa ";
name = name.trim();
name = name.toUpperCase();
console.log(name.toUpperCase());

console.log(name.split(" "));

let fullname =  `si ${name} Gustavo play
gta v`;
console.log(fullname);